# secrets / entorno
.env
.env.*
!.env.example
serviceAccount*.json
*firebase-adminsdk*.json

# deps
node_modules/

# datos derivados (generados con npm run build:data — pesados, no versionar)
data/
*.slim.ndjson

# volcado JSONL de páginas (~155 MB, fuente de la ingesta — no versionar)
contenido_paginas.jsonl
*.jsonl

# ingesta
ingest-checkpoint.json

# logs
*.log

# vercel
.vercel/
